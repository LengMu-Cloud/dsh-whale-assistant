# 🐳 dsh-whale — DeepSeek Harness 小鲸鱼陪伴助手

一个住在 DeepSeek Harness Web UI 右下角的打工小鲸鱼：帮你盯着任务进度、汇报 token 消耗、
定时叫你起来干活、在你忙不过来的时候卖萌解压。它只**读**任务事件流，不改动任何任务本身，
完全独立运行。

---

## 一、基本操作（手势速查）

| 手势 | 功能 |
|---|---|
| **左键拖动** | 移动小鲸鱼到屏幕任意位置（位置自动记住，刷新不丢） |
| **左键单击** | 互动：有未读通知时读一条；没有时汇报当前任务状态 / 说句闲话（伴随一圈水波涟漪反馈） |
| **左键双击** | 游回默认角落（游泳动画 + 水花；游到一半抓住它会有彩蛋台词） |
| **右键单击** | 打开菜单（见第三节） |
| **右键双击** | 摸摸头：粉色爱心 + 撒娇台词 + 好感度 +1 |
| **红标（角标）单击** | 逐条阅读未读通知（最新优先） |
| **红标（角标）双击** | 一键清空所有未读（鲸鱼旁淡出「已清空 N 条通知」确认） |
| **通知气泡双击** | 跳转到该通知对应的对话，且该通知自动标记已读（红标 -1） |
| **通知气泡悬停** | 鼠标停在气泡上时暂停倒计时，移开后按剩余时间继续消失（保底 1.5 秒） |

> 完整图文版在页内：右键菜单「📖 操作说明」，顶部还有搜索框。

---

## 二、任务通知系统

小鲸鱼通过插件的事件流实时监听任务动态（活跃窗口 DOM + 服务端轮询双通道），**只通知需要你关注的事**：

| 事件 | 气泡文案 | 音效 |
|---|---|---|
| **主任务开工** | 「任务「xx」开工了 🐋」 | 无声（避免打扰） |
| **主任务完成** | 「任务「xx」完成了 🎉」 | 自行车铃铛「叮-叮」 |
| **需要你审核 / 回答** | 「⚠️ [会话名]需要你审核」等 | 上行三连音 |
| **任务失败** | 失败原因播报 | 下行低沉音 |
| 后台子任务 / 子代理 | **完全静默**（只影响疲劳度） | 无声 |

- **后台会话也覆盖**：别的窗口 / 标签页里跑的任务照样通知（服务端 3 秒轮询兜底）。
- **批量折叠**：浏览器限流导致切回标签页时积压的一堆完成通知，自动折成一条摘要，红标不爆炸。
- **长任务计时**：任务超过 2 分钟后，状态面板出现「⏳ [对话名] mm:ss · 已完成 N 个子任务」——多任务并跑时**每个会话各占一行**（按开工顺序），名字超宽自动截断，每秒刷新，完成即停。
- **双击气泡跳转**：跳转失败（会话已清理）会提示且通知保留。

---

## 三、右键菜单

右键单击小鲸鱼弹出深色半透明菜单（自动避让屏幕边缘），按层级分成两组：

| 分组 | 菜单项 |
|---|---|
| **快捷操作** | ⏰ 提醒我 ｜ 🔊 声音：开/关 |
| **信息与设置** | 📖 操作说明 ｜ 🎨 我的装扮 ｜ 🗂️ 历史任务 ｜ ⚙️ 设置 |

- 菜单顶部一行**实时状态**：当前班味心情 + 进行中任务数。
- **所有子面板统一页脚**：从菜单进的（提醒我 / 装扮 / 使用说明 / 历史任务 / 设置）都有
  「← 返回菜单」，从设置进的子面板（音色 / 周报 / 运行状态）都有「← 返回设置」，
  右侧 ✕ 随手关闭——不用关了重开。

---

## 四、定时提醒 ⏰

右键菜单「⏰ 提醒我」打开提醒面板，两种定法：

- **倒计时**：5 / 10 / 25 / 60 分钟一键选档。
- **指定时刻**：时 / 分 / 秒三个紧凑步进器（−/+ 加减，也可以直接键入数字，越界自动环绕/夹紧），
  点「添加提醒」生效；时间已过会自动顺延到明天。
- **待响列表**：每条提醒显示「M月d日 HH:MM:SS（还剩约 N 分钟，点按取消）」，点一下即可取消。
- **持久化**：提醒存本地，刷新页面不丢；到点响铃（音色可在设置里换）+ 气泡播报。

---

## 五、音效系统 🔊

全合成音效（Web Audio API），无音频文件。**每类通知可单独选音色**（设置 → 音色面板）：

| 通知类别 | 可选音色（默认加粗） |
|---|---|
| 任务完成 | **ding** / bell / chime / 静音 |
| 审核 / 提问 | **chime** / ding / bell / 静音 |
| 任务失败 | **thud** / ding / bell / chime / 静音 |
| 定时提醒 | **bell** / ding / chime / thud / 静音 |

- 下拉选中即生效并试听一声。
- **排队播放**：连续通知音效排队（间隔 1.1 秒），绝不重叠；突发保护保留最新 5 个。
- **一键静音**：右键菜单「声音：关」全局静默（气泡照常），状态持久化。
- **免打扰时段**：设置里可配深夜时段，静音完成/失败音（跨零点支持）。

**未读红标**：所有通知累积成红标数字（最多 99+）。单击读一条、双击全清空、双击气泡跳转并自动已读。

---

## 六、历史任务与周报 🗂️

右键菜单「🗂️ 历史任务」打开历史抽屉：

- **搜索**：关键词过滤 + 命中高亮 + 「找到 N 条记录」计数；**大小写不敏感**（输 step 也能搜到 Step）。
- **范围清空**：清空按钮展开三档——全部 / 仅 7 天前 / 仅 30 天前（二次确认）；清空同步到
  所有窗口，已清的记录不会因为后台通知回流而复活。
- **📇 清空通讯录**（0.3.3）：二次确认后抹掉全部认名。**彻底遗忘某个对话是两步**——
  先清历史（删掉记录里的名字快照）、再清空通讯录；只做后一步的话，历史快照会在
  下次加载时把名字带回来。清空后名字回退「未命名任务」，遇到会自动重新学习。
- **认名簿（通讯录）**：鲸鱼记住的对话名独立存储（浏览器 localStorage + 本机服务端
  镜像跨窗口共享，不跨机器），容量 500 条按最近使用淘汰——「清历史不清名字」指
  **不主动删**，长期不用的名字在容量压力下仍会被动淘汰。
- **任务周报**：近 7 天按日统计，支持导出 Markdown / CSV。
- 历史存在本机 `~/.dsh/whale-assistant.json`，同机所有窗口/浏览器共享一份。

---

## 七、使用说明手册 📖

右键菜单「📖 操作说明」打开侧栏大抽屉：

- **🚀 快速上手**：新用户三分钟上手路径，置顶。
- 8 节完整说明（通知一览 / 手势 / 设置项详解 / 红标语义 / 彩蛋……）。
- **🙋 常见问题**：高频疑问速查，收尾。
- **顶部搜索框**：关键词即时过滤 + 高亮 + 计数，未找到有明确提示。

---

## 八、摸摸头 & 好感度 ❤️（右键双击）

- 粉色爱心从头顶飘起 + 随机撒娇台词 + 开心弹跳
- **好感度 +1**（持久化）
- 三层台词：0~9 初次互动 → 10+ 熟悉 → 50+ 挚友
- **里程碑庆祝**：好感度达到 10 / 50 / 100 时，一次性头顶爱心雨 🎉（各庆祝一次）
- **闲置冒心**：闲置一段时间且好感度够高时，头顶会缓慢冒出小爱心（1~3 颗按好感度分层）
- **引导台词**：长期闲置时偶发提示隐藏玩法（如摸摸头手势），每天最多 3 次，睡觉/免打扰/忙时不打扰

---

## 九、班味情绪系统 🫠

小鲸鱼会像打工人一样逐渐疲惫，四级渐变：

| 等级 | 名称 | 累计工作量 |
|---|---|---|
| 1 | 元气满满 | 0+ |
| 2 | 渐入佳境 | 4+ |
| 3 | 有点班味 | 9+ |
| 4 | 班味十足 | 16+ |

任务开始/失败会加重班味（+1），完成会缓解（-0.5）。疲惫后台词变丧、完成时叹气、被抓时躺平认命。

---

## 十、每日装饰成就 ⛑️

装饰是**每日成就**：每天清零重计，第二天装饰消失，需要重新积累。

| 装饰 | 解锁条件 |
|---|---|
| ☕ 小咖啡杯 | 当天完成 5 个任务 |
| ⛑️ 红色安全帽 | 当天完成 15 个任务 |

- 解锁时小鲸鱼会宣布「解锁新装饰」；装饰画在 SVG 内部，游泳/转身时自动跟随
- 跨过午夜自动重置；当天进度在「🎨 我的装扮」面板可查

---

## 十一、Token 报告 & 状态面板 📊

任务完成时，鲸鱼旁边的**状态面板**（深色小卡片，自动选放下方、左侧或上方）显示三行：

- **此次任务消耗** X tokens（本回合实际消耗，与官方芯片数字一致）
- **全对话累计消耗** X tokens（会话总消耗）
- **上下文已用** X%（上下文压力。0.3.3 起数据走 DSH 的 token-meter 服务端投影，
  不再依赖页面文字——0.1.5 把内联百分比改成发送按钮旁的环形件后，此行不受影响；
  点环形件可看 系统提示词/工具定义/对话消息 官方明细）

面板特性：
- 随鲸鱼位置实时调整：拖动时自动收缩/翻转，永不超出屏幕（鲸鱼贴近屏幕底部时面板自动翻到鲸鱼**上方**）；超高面板内部滚动
- **互不覆盖的三框体系**：⚠️ 工具卡住提示、⏳ 长任务计时、📊 完成报告同时到来时各占其位——提示占主框，计时落入主框下方副框，报告滑入主框旁侧旁路框，各按自己的寿命到点消失
- **工具卡住提示（看门狗）**：工具超过 30s 且该会话没有在跑的作业时提醒「⚠️ [对话名] 工具已运行 Ns」，多会话卡住逐行并列；清理信号丢失的残留条目超过约 5 分钟自动清除。**权衡**：真跑了超过自清线（10× 阈值，默认 5 分钟）的工具，其 ⚠️ 也会按僵尸规则退场（完成报告不受影响）——常跑长工具就在设置里调大「工具卡住阈值」
- 单击读回历史通知时，重新显示该通知当时的数字
- **压力警告**：上下文 ≥70% 时提醒「上下文快挤爆了…建议 /compact 🫠」（一次性，缓解到 50% 以下重新武装）
- **长任务计时行**：见第二节

---

## 十二、运行状态（健康自检）🩺

小鲸鱼每 60 秒自检一次三大依赖：服务端事件轮询 / 用量 DOM 读取 / 会话跳转钩子。

- 一切正常时**零痕迹**（不打扰）；
- 检测到失效会在鲸鱼旁出现 ⚠️ 小图标，点开看逐项明细；
- 控制台同步输出 `[🐋] Health check: {...}`；多数失效一次 Ctrl+F5 即可恢复；
- 设置 → 诊断组 →「🩺 运行状态」也可随时手动查看。

---

## 十三、设置面板 ⚙️

右键菜单「⚙️ 设置」，11 行按五组分区（上方分隔线 + 宽字距组标题）：
**通知 / 声音 / 监控 / 数据 / 诊断**。

- 每行末尾的 **(?)** 点击展开一句话说明，再点「收起 ▴」收回。
- 诊断组：🩺 运行状态（自检详情）、🪲 调试模式（取证数据写 `_debug`）。
- 面板底部「← 返回菜单」。

---

## 十四、睡眠模式 💤

- 5 分钟没有任何活动 → 自动入睡（变灰 + 💤 漂浮 + 「呼…先睡会儿 Zzz」）
- **唤醒方式**：任务活动、左键双击（游泳）、拖动、右键互动
- **单击不唤醒**：睡觉时单击只会得到「呼… Zzz 别吵我 🥱」

---

## 十五、其他细节

- **断线重连**：事件通道断开后自动指数退避重连（1s → 15s），状态不丢
- **偏好记忆**：位置、声音开关、音色选择、好感度、提醒、每日任务数都存本地，刷新不丢
- **安全**：只读事件流，不拦截、不修改应用任何行为；服务端接口仅回环地址可访问

---

## 十六、安装与开发（给开发者）

安装见 [README.md](README.md)（cordis 插件注册 + 可选桌面壳增强）。常用命令：

```
node build-whale.js     # 重建 whale.js + whale-map.md，并同步发布副本到 whale-assistant/lib/
node test-whale.js      # 单元测试（vm 沙箱 640+ 断言）
node scripts/run-e2e.js # 端到端回归
```

调试与测试用的导出缝清单见 [docs/seams.md](docs/seams.md)；版本历史见 [CHANGELOG.md](CHANGELOG.md)。

---

*小鲸鱼永远待命，随时汇报 🐋✨*
