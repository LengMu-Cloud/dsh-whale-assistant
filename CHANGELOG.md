# Changelog — dsh-whale

所有对外可感知的变更按版本记录。当前版本见 `src/utils/constants.js` 的 `PATCH_VERSION`
（构建时可用 `PATCH_VERSION=0.4.0 node build-whale.js` 覆盖）。

## [0.3.3] - 2026-09-13

上下文压力复活包（0.1.5 适配债清偿）+ 通讯录「彻底遗忘」入口。

### 修复
- **上下文压力三处同源复活（报告压力行 / 压力描边色 / ≥70% 提醒）**：0.1.5 移除「上下文已用 N%」DOM 文本后三处一起失供——根因是 `server-events` 沿用 0.1.2 时代的「活跃会话投影帧丢弃」守卫（当年 DOM 喂给覆盖得上），0.1.5 下 DOM 无料可喂，活跃会话（提醒唯一读取对象）被自己的守卫掐死。现在 `contextPressure` 投影帧不再跳过活跃会话（`tokenUsage` 维持跳过，累计条 DOM 优先不变），数据改走 **dsh-token-meter 服务端投影**（dsh-base roster 常驻插件，`request/context` + `assistant/message` usage 权威计算）——官方 UI 再怎么改版都不影响供数
- **宿主半区同修（投影订阅从未触发的真凶）**：`index.js` 裸取 `ctx.sessionProjections?.onChanged`——投影服务与本插件激活顺序无保证，服务未挂载时 `?.` 把整个订阅静默吞掉（推送后真机验尸：真实任务后事件环 20 帧、**零投影帧**——转发代码存在但从未触发；投影服务文档明确要求 `inject` 声明）。已照 jobs 订阅同款改为 `ctx.inject(['sessionProjections'],…)` 就绪后挂载。**宿主半区改动需重启 DSH 服务生效**
- **百分比口径对齐官方 ContextMeter**：新增 `pressurePercentOf` 纯函数（status-panel 与 reports 同一公式）：`projectedTokens`（采样 + 表面漂移）优先、`pressureTokens` 兜底、`Math.min(100)` 截断、**窗口无采样返回 null 不造假 0%**；旧版 DOM 百分比形状经同一公式不变

### 新增
- **📇 清空通讯录**（历史抽屉，两段式确认）：补上「彻底遗忘某个对话」的缺失入口。真正遗忘 = 两步：先清历史（删掉记录里的名字快照），再清空通讯录（抹掉名字簿 + 内存名，localStorage 键直接移除）——顺序反了历史快照会在下次加载时回流。清空后名字回退「未命名任务」自纠路径，可重新学习；手册行同步两步语义

### 测试
- 压力复活集成例：服务端轮询路径投喂**活跃会话** contextPressure → ≥70% 触发提醒；`pressurePercentOf` 八态（projectedTokens 优先 / pressureTokens 兜底 / 旧形状兼容 / 无数据 / 无形状 / 无采样不造假 / 100 截断 / 真实 0 样本）；`_clearTitleBook` 生命周期（入簿 → 清空 → localStorage 键移除 → 名字遗忘 → 重新学习）

## [0.3.2] - 2026-09-12

状态面板「互不覆盖」专项：⚠️ 工具卡住提示、⏳ 长任务计时、📊 完成报告在小鲸鱼旁各占其位，同时可见、互不顶掉。

### 新增
- 🪜 **计时器拆框同屏（用户拍板 2026-09-12）**：⚠️ 工具卡住提示与 ⏳ 长任务计时原来抢同一个状态面板（计时器"让路"，两者不能同屏）——现在主面板被报告/提示占用时，计时行自动落入**独立副框**（紧贴主框下方，随主框侧别走、空间不足自动上翻），主框空闲则回主框；工具超时提示在上、长任务计时在下，同时可见。真机验证：150s 任务在 2min18s 时两框同屏（`⚠️ 工具运行中（136s）` + `⏳ [会话名] 02:18`）
- 🚨 **卡住提示多会话并列 + 全程归属（用户拍板 2026-09-12）**：文案改为 `⚠️ [对话名] 工具已运行 Ns`——每个卡住超阈值的会话**各自一行、逐行堆叠**（最久的在前，名字超宽 … 截断），与 ⏳ 计时同屏时靠拆框各占一位；配套两个追踪缺口修复：①**后台会话的工具纳入看门狗**（server-events 补 `_trackTool`/`_clearTool` 缝，只追踪不闪 🔧 面板）；②**工具行豁免 switchRender 拦截**（首次渲染恰好落在切换/冷却窗口的新调用曾被永久漏报——seenCallIds 按 callId 去重已足够防双响，🤔 提问保留全量拦截）。真机双会话验证：两行提示并列 → 2 分钟后双行计时同屏 → 各会话完成逐个退场
- 📄 **报告旁路框（用户方案 2026-09-12）**：⚠️ 提示挂起期间到达的任务完成报告不再顶掉提示，而是滑入主框**旁路框**（优先主框左侧，无空间依次回退上方/右侧），与提示同屏互不覆盖，到点自灭；无提示挂起时报告照旧回主框。真机验证：`📊 此次任务消耗 22.9K tokens` 与 `⚠️ [会话名] 工具已运行` 同屏截图
- ⬆️ **贴底翻上方**：鲸鱼贴近视口底部、或面板高于下方剩余空间时，状态面板自动翻到鲸鱼**上方**（`dsh-whale-status-above` 变体），不再被屏幕下缘裁切（用户报告 09-12：鲸鱼贴底面板显示不全）。真机验证：距底 85px + 4 行面板翻上方截图

### 修复
- **0.1.5 会话累计用量文案适配**：官方统计条由「输入 X tok · 输出 Y tok」改为「`10.8M tok·缓存命中 6%`」。`feedSessionUsage` 在保留旧正则的同时识别新格式，避免全对话累计静默失效。**已知降级**：0.1.5 输入框旁「上下文已用 N%」已从 DOM 移除，前台上下文压力色可能不再更新（完成气泡旁本轮 token 面板仍正常，依赖 `assistant/message` usage 与「用量 X tok」芯片）
- **0.1.5 累计条拆节点适配（真机回归修复）**：0.1.5 实际 DOM 把总量与缓存命中渲染成**两个独立文本节点**（`<span>10.9M tok<span>·</span>缓存命中 7%</span>`），节点级文本永远不同时包含两半——首版仅按节点文本匹配的正则真机不生效，全对话累计行静默消失（2026-09-11 真机验收抓到）。修复：节点含 `tok`/`缓存命中` 时对**父节点 textContent** 重试组合匹配（每轮芯片父节点无「缓存命中」，不会误报）；新增 `_feedSessionUsage` 测试缝与单测组 60（拆节点/合节点/芯片不误报三态锁死）
- **卡住提示双 ⚠️ 去重**：`.dsh-whale-status-stuck` 的 CSS `::before` 装饰与提示文本自带的 ⚠️ 叠加成「⚠️⚠️」（该提示极少触发，一直没人看见）——删 CSS 装饰保留文本 ⚠️，红边框不变
- **设置行「工具超时」图标对齐**：⏱（U+23F1）默认是文本呈现符号，缺 U+FE0F 变体选择符时渲染成瘦小单色字形、与其他彩色 emoji 图标大小/基线不齐——补 VS16 恢复 emoji 呈现（设置行 + 手册行两处）
- **僵尸条目自清**：工具/轮次的清理信号丢失时（帧在重载前后被丢），残留的卡住条目会在屏上挂几分钟无人能处置（用户截图曾见 610s/339s 幽灵）——超过 10× 阈值（≥300s）由看门狗自删，活着的邻居不受影响
- **扫法杀框（真机验收抓到）**：⚠️ 提示每 2s 的自刷新顺带清掉旁路框——旁路报告刚出现就在下一个扫法拍被杀（实测 22.4K 报告存活不足 2s）。修复：提示/计时**接管**主框不再清旁路框，只有真实内容接管才清；旁路报告按自身 6s 寿命走完
- **气泡 1:1 连带守卫**：气泡出现/隐藏连带隐藏状态面板的旧约定（09-02 定稿"面板随气泡同灭"），在旁路框时代会误杀旁路报告、闪掉 ⚠️ 主框——晚到的开工播报落在报告 6s 窗口内即触发。修复：提示在岗期间气泡的 hide 不再触碰面板（主框由提示守着，旁路框有自己的寿命定时器）
- **🖥️ 壳冷开机黑屏（旧相识，cookie 快路径竞态）**：重启电脑后首次双击 DSH 黑屏、手动刷新一次才好，同次开机内重启壳不复现。根因：dsh web 先开 TCP 端口、cordis 插件后挂载，而登录过一次的机器 Electron 会话 cookie（30 天、落盘）永远存在 → cookie **快路径**跳过插件就绪检查、在挂载窗口内直接 loadURL → 渲染出空页面；关窗不杀后台服务，所以只有冷开机才撞上这个窗口（慢路径的就绪检查一直在，但从未被执行）。修复：快路径加载前也真实探测 `/api/whale-assistant/state` 200——温启动毫秒级通过、秒开体感不变；冷开机在挂载窗口内每秒重试；约 20s 超时降级加载（登录页兜底，不让插件故障卡死壳）。**壳补丁需重打**（`scripts/fix-shell.ps1`，幂等）+ 重启壳生效

### 测试
- 单测组 40c：报告旁路（出现/跨扫法拍存活/按期退场/退场后回主框）+ 僵尸自清 + 晚到 say 守卫；40 组补 stuck 提示 `_stuck` 自识别（否则被自家分流逻辑赶进旁路框）；测试 DOM `appendChild` 补 `parentNode` 回填（`ensureReportEl`/`ensureTimerEl` 的复用判断依赖它，缺了会在 vm 里造出重复框）
- 真机验收（2026-09-12）：报告旁路同屏截图、僵尸 310s 槽进扫法即删、贴底 above 变体（4 行面板整体翻上方、底边离鲸鱼顶 12px）、诊断探针零残留

## [0.3.1] - 2026-09-11

DSH **0.1.5-rc.2** 升级适配（本机由 0.1.2-rc.1 一键/手动升级实测）。

### 修复
- **0.1.5 会话累计用量文案适配**：官方统计条由「输入 X tok · 输出 Y tok」改为「`10.8M tok·缓存命中 6%`」。`feedSessionUsage` 在保留旧正则的同时识别新格式，避免全对话累计静默失效。**已知降级**：0.1.5 输入框旁「上下文已用 N%」已从 DOM 移除，前台上下文压力色可能不再更新（完成气泡旁本轮 token 面板仍正常，依赖 `assistant/message` usage 与「用量 X tok」芯片）
- **0.1.5 累计条拆节点适配（真机回归修复）**：0.1.5 实际 DOM 把总量与缓存命中渲染成**两个独立文本节点**（`<span>10.9M tok<span>·</span>缓存命中 7%</span>`），节点级文本永远不同时包含两半——首版仅按节点文本匹配的正则真机不生效，全对话累计行静默消失（2026-09-11 真机验收抓到）。修复：节点含 `tok`/`缓存命中` 时对**父节点 textContent** 重试组合匹配（每轮芯片父节点无「缓存命中」，不会误报）；新增 `_feedSessionUsage` 测试缝与单测组 60（拆节点/合节点/芯片不误报三态锁死）

### 变更
- **`run-update.ps1` 对齐 0.1.5 实装路径**：插件 junction 改为 `@lengmu-cloud/dsh-whale-assistant`（修复 0.3.0 改名后仍写 `@deepseek-ai` 的路径 bug）；钩子校验改为「已知候选 + 递归搜索」，找不到 conversation 插件时 **exit 1**（原先 WARN 后继续会假绿）；npm 安装改为绝对路径 `node`+`npm-cli.js` + `--ignore-scripts` + 手工编译 koffi（本机 0.1.5 实装：koffi 子进程 `cmd /c node` 找不到 node 导致裸 `npm i -g` 失败）；`dsh --version` 改为优先读 package.json
- **`apply-plugin-hook.ps1`**：`npm prefix -g` 探测改为 try/catch（npm.ps1 在 LASTEXITCODE 未设置时会抛错中断注入）；conversation 候选路径补充 `dsh-web` 嵌套

### 实测（0.1.5-rc.2 真机）
- 宿主 `session/event` 总线仍在：`turn/start` / `turn/end(completed)` / `agent/inbox/spliced` / `assistant/message` / `model/selection` 正常入环形缓冲
- 完成气泡「[对话名]完成了 🎉」+ 历史 `kind=done` + `turnTokens`（与官方「用量 14.7K tok」一致）
- 客户端 `__ModuleLoader__` / `#dsh-whale` / `serverHealth.failStreak=0`
- 钩子锚点 `const sessions = ctx.sessions` 与 `loadOlder` 在 0.1.5 构建产物中仍存在并可注入
- `test-whale.js` 全绿

## [未发布] - 2026-09-08

文档与装机链路修正（无功能变更）。

### 修复
- **`ensure-whale-assistant.ps1` 注册行补 scope**：写入 `cordis.patch.yml` 的 `name:` 由无 scope 的 `dsh-whale-assistant` 改为带引号的 `'@lengmu-cloud/dsh-whale-assistant'`（alpha client 清单只聚合带 scope 的包，此前全新机器照脚本安装不会生效）；已用一次性临时 profile 实测首次写入与幂等重跑

### 变更
- **README 全面更新**：简介采纳 ①-⑩ 功能清单；新增环境要求 / 升级与卸载 / 工作原理 / 常见问题 / 开发与测试五节；安装节接入 ensure 脚本 + junction 建链完整三件套，注册行示例改为真实 FLAT 语法；能力矩阵补陪伴系统与历史跳转；测试断言数 570+ → 620+（与 FEATURES.md 同步）

## [未发布] - 2026-09-06

GitHub 开源发布准备（内部清理，无功能变更）。

### 变更
- **插件包 scope 改名** `@deepseek-ai/dsh-whale-assistant` → `@lengmu-cloud/dsh-whale-assistant`：包 name、client 模块 id、cordis.patch.yml 注册行、ensure 脚本、README 示例同步更换；0.3.0 及更早条目保留旧名为历史记录。路由 `/api/whale-assistant/*`、数据文件与注册点 id `ui-whale-assistant` 不变
- 包版本对齐 0.3.0，补 author / license / repository 元数据
- 装机脚本可移植化：`ensure-whale-assistant.ps1` 参数默认值动态化；`apply-plugin-hook.ps1` 的 DSH 包定位改 `npm prefix -g` 动态解析；`lib/index.js` 移除本机路径兜底（包内副本即唯一来源）；`fix-shell.ps1` 移除本机安装位置与 asar 维护目录兜底；`build-whale.js` 不再强依赖本地参考件 `_whale.orig.js`（release 克隆降级为清单自洽校验）

## [0.3.0] - 2026-09-05

体验优化 14 项（外部方案评审落地 13 项）+ 三轮用户反馈十项。

### 新增
- ⏳ **长任务计时**：任务超过 2 分钟后状态面板出现「已运行 mm:ss · 已完成 N 个子任务」，完成即停；**多任务归属（09-06）**：改为每个在跑会话一行「⏳ [对话名] mm:ss · 已完成 N 个子任务」，名字超宽自动 … 截断，并发的 🔧/📋 工具提示也带上会话名，且计时行不再踩掉完成报告面板
- 📇 **名字通讯录**：学到的对话名存独立通讯录（LRU 500 条），清空历史不再导致"失忆"——清空+刷新后的第一条通知也带真名
- 🏷️ **开机主动取名**：重载进旧对话时直接读页面标题喂给当前会话，开工/提问无需等名字生成
- 💧 **气泡悬停暂停**：鼠标停在通知气泡上暂停倒计时，移开按剩余时间继续
- 📭 **红标清空反馈**：双击清空后淡出「已清空 N 条通知」
- 📖 **手册搜索框**：操作说明顶部关键词过滤 + 高亮 + 计数，新增「🚀 快速上手」「🙋 常见问题」两节
- 🔍 **历史搜索高亮**：关键词高亮 + 「找到 N 条记录」，搜索大小写不敏感
- 💬 **摸摸头引导台词**：长期闲置时偶发提示隐藏玩法（日上限 3 次，四重门控）
- ❤️ **好感度爱心**：闲置分层冒心 + 里程碑（10/50/100）一次性爱心雨庆祝
- 🗑️ **清空历史范围化**：全部 / 仅 7 天前 / 仅 30 天前，三档二次确认
- 💧 **单击涟漪**：点击鲸鱼的水波光晕反馈
- ⏰ **指定时刻提醒**：时/分/秒步进器，已过自动顺延明天；到点响铃
- 🔔 **提醒音效可选**：新增「定时提醒」音色类别（默认 bell）
- 🧭 **子面板返回导航**：设置子面板「← 返回设置」、提醒/设置面板「← 返回菜单」
- ❔ **设置 (?) 说明浮窗**：11 行设置全部带点击展开的一句话说明与「收起」

### 变更
- **插件全量改名** `@deepseek-ai/dsh-whale-store` → `@deepseek-ai/dsh-whale-assistant`（发布前定名）：目录、包名、注册点 id、全部 API 路由（`/api/whale-store/*` → `/api/whale-assistant/*`）、数据文件（`~/.dsh/whale-store.json` → `~/.dsh/whale-assistant.json`，自动迁移旧数据）同步更换；0.2.0 条目保留旧名为历史记录
- 右键菜单分组：快捷操作（提醒/声音）｜信息与设置（说明/装扮/历史/设置）
- 设置面板按五组分区（通知/声音/监控/数据/诊断）；「健康自检」入口移入诊断组并改名「🩺 运行状态」；「诊断模式」改名「调试模式」
- 音色面板：铺满一屏的 13 行列表改为每类通知一行下拉，选中即换即试听
- 提醒面板：指定时间从手填输入框改为单行步进器；「➕」图标改为文字行
- 下拉选项深色配色（白底浅字可读性问题）
- DSH 0.1.2-rc.1 适配：`run-update.ps1 -Channel rc`、`apply-plugin-hook.ps1` 候选路径+递归兜底

### 修复
- 提问音色选「静音」后仍会响内置琶音
- 设置 (?) 展开说明、清空历史展开选项后面板底部被截（展开后重新定位）
- 指定时间提醒「添加」按钮重复注册导致一次加两条
- 历史过滤改大小写不敏感后高亮条件未同步
- 清空历史后后台回流通知使记录「复活」（clearAt 时间戳同步）
- 「需要你选择/审核」在对话名未生成时把问题/审批原文当作对话名显示，且该文本被存入历史记录标题、每次刷新经标题预填再次污染（清空历史后最易复现）——改为「未命名任务」中性占位，历史只存真实对话名
- 「需要你选择/审核」通知不再弹 tokens 用量面板（任务未完成，读数为时尚早），用量面板只跟完成/失败
- 右键点未读红标会误触读通知（与右键菜单叠加）——红标只认左键，右键只开菜单
- 我的装扮 / 使用说明 / 历史任务三个面板补齐「← 返回菜单」页脚（与其他子面板统一）
- 红标读取提问/审核通知时用量面板复活（自动弹出已封、读取回放没封）——存队列时不再带任何 token/压力数据，读取只回放文字
- 同一次任务完成通知双响（问答流中活跃会话判定竞态，DOM 芯片与轮询帧各说一遍）——完成通知改为按事件时间 ±8 秒做跨通道去重：同一物理结束只说一次，真正的新完成照常播报
- 手动停止任务后「⏳ 已运行」计时仍在走（活跃会话的中止事件被轮询门丢弃，而被杀的轮次不渲染用量芯片，DOM 无停止信号）——中止事件现在对活跃会话放行：计时即停，并恢复「被中止了 ✋」播报与 ⏹ 历史行（后台会话原本正常）
- 页面重载后旧事件帧被重放，产生重复历史记录与重复用量累计（seq 存 sessionStorage 随刷新丢失）——seq 迁移 localStorage 持久化 + 完成记账死守卫
- 页面重载前已开工的轮次，其完成通知静默丢失（DOM 芯片未武装 + 活跃会话门丢弃 completed）——completed 放行并以事件时间 ±8s 去重吸收双响
- 短任务完成后「⏳ 计时行」永久泄漏（完成通知去重路径跳过了停表）——停表提到事件门级最先执行
- 2 个以上任务并跑时，看门狗「⚠️ 工具运行中」行不带会话名（其余状态行已带）——补齐
- 对话标题在 rc.1 以 session/title 事件帧广播而插件未消费，后台/提问场景通知名长期依赖兜底——消费该事件并接入通讯录与三处改写，浏览器新开页首条通知即带真名

## [0.2.0] - 2026-09-04

插件化重构 + 后台通知体系 + 分发准备（DSH 0.1.2-alpha ~ rc.1 期间）。

### 新增
- 📦 **官方插件包**：双半区 cordis 插件（`@deepseek-ai/dsh-whale-store`），DSH 升级/重装不再丢鲸鱼
- 🖥️ **桌面壳补丁**：token 认证加载 + 303 就绪验证 + 插件金丝雀 + 崩溃自恢复 + 服务守护；`backgroundThrottling:false` 关闭后台限流
- 🛠️ **安装/升级脚本**：`fix-shell.ps1`（幂等壳安装器）、`run-update.ps1`（一键升级恢复）、`ensure-whale-store.ps1`（插件注册）
- 📡 **后台会话通知**：服务端事件 + 3 秒轮询双通道，其他窗口/标签页的任务全覆盖
- 🔢 **后台每轮真实用量**：面板数字与官方芯片一致
- 🔀 **失败双通道**：DOM 即时播报 + 服务端权威失败原因
- 📉 **五层闸门纯函数** + DOM 适配器：零假信号，独立单测
- 📛 **标题预填**：重载后后台通知也显示真实任务名
- 📦 **批量折叠**：限流标签页切回后积压通知折为一条摘要
- ⚙️ **设置面板**（五组 10 行）、**音色按通知类别选择**、**免打扰时段**（跨零点）
- ⏰ **定时提醒**（倒计时 5/10/25/60 分钟，持久化）
- 🗂️ **历史抽屉**（搜索 + 二次确认清空 + 云合并去重）、**任务周报**（近 7 天，Markdown/CSV 导出）
- 🩺 **健康自检**：依赖失效 ⚠️ 告警 + 详情面板，正常零痕迹
- 📖 **使用说明手册**：侧栏大抽屉 8 节
- 🪲 **调试模式**：取证数据写 `_debug`；**dev-say 回环注入路由**（无人值守回归）
- 🧭 **分发四件套**：README 能力矩阵 / docs/shell-setup.md / whale-map.md 函数导航 / CHANGELOG

### 移除
- index.html 注入方案（apply.ps1 / apply-plugin-hook.ps1 注入 dist 路径）——被插件包取代，升级免疫

## [0.1.0] - 2026-08-31

v1 时代：37 项功能的初始版本（index.html 补丁形态）。

- 交互 8：移动 / 互动 / 游泳 / 抓取 / 菜单 / 摸摸头 / 红标读 / 红标清空
- 通知 6：开工 / 完成 / 审核 / 提问 / 子任务静默 / 子代理静默
- 音效 5：完成铃 / 审核上行 / 失败音 / 排队播放 / 突发保护
- 情绪 4：四级班味 / 疲惫台词 / 完成叹气 / 被抓躺平
- 装饰 3：咖啡杯 / 安全帽 / 跨午夜重置
- 面板 4：三行报告 / 状态面板 / 压力警告 / 帮助面板
- 系统 5：声音开关 / 睡眠 / 好感度 / 断线重连 / 偏好记忆
- 文档 2：操作说明 / 装扮面板

---

*小鲸鱼永远待命，随时汇报 🐋✨*


## [未发布] - 2026-09-09

### 修复
- **桌面壳启动慢（服务已在运行时约 60s）**：`shell/main.js` 的 `loadUiWithAuth` 改为 cookie 快路径优先（`session.defaultSession.cookies`）；无 cookie 时 token 探测死等 60s→8s（`AUTH_FAIL_LIMIT`），鲸鱼插件金丝雀窗口 15s→4s。根因：Node `http.get` 不带 Electron cookie，且日志中一次性 token 在服务已运行时已消费（恒 401）。

### 变更
- 仓库路径：`whale-patch` → `D:\vibe coding\dsh-whale-assistant`（绑定/Junction/更新脚本已改；GitHub remote 不变）
- 报告文件名日期更新：`设计总结汇报-2026-09-09.md`、`交接报告-最终-2026-09-09.md`
